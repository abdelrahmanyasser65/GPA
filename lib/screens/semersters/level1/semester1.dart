import 'package:flutter/material.dart';
import 'package:gpacalculate/components/constants.dart';
class Semester11Screen extends StatelessWidget {
  const Semester11Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(titleScreens[1],
          style:appBarStyle),
      ),
      body: null,
    );
  }
}
